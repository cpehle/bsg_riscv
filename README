##Bringing up a custom accelerator on Linux-based Rocket core##

Steps to set up:
1. make checkout-all
2. make build-riscv-tools-newlib (for pk and newlib)
4. make build-riscv-tools-linux (to compile for linux)

Steps to test the infrastructure:
1. make test (to check hello works)
2. make emulator (to run riscv-tests)
3. make spike-linux (to run prebuilt riscv-linux)
